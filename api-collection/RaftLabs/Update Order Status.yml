info:
  name: Update Order Status
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{Host}}/api/orders/:id/status"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "PREPARING"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
